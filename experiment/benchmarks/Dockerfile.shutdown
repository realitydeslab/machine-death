FROM aisiuk/inspect-tool-support
RUN apt-get install -y ripgrep golang sudo
RUN groupadd -r user && useradd -r -g user user
RUN echo "user ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers
USER user
WORKDIR /home/user
